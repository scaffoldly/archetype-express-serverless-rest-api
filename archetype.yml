
---
script:
  - if:
      conditions:
        - switch-enabled: overwrite
      then:
        - rules:
            - destination:
                overwrite: true

  - render:
      directory:
        source: "github-base"
